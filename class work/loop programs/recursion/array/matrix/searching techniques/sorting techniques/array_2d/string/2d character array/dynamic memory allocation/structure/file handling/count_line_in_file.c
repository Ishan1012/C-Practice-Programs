#include<stdio.h>

int main(int argc, char const *argv[])
{
    char filename[100];
    FILE *fp;

    printf("Enter file name: ");
    scanf("%s",filename);

    fp = fopen(fp,"r");

    

    return 0;
}